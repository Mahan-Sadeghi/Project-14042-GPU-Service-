# سیستم مدیریت منابع پردازشی (GPU Service Platform)

این پروژه، یک سیستم بک‌اند (Backend) برای مدیریت و تخصیص منابع پردازشی (GPU) است که به عنوان پروژه درس **برنامه‌نویسی پیشرفته** توسعه داده شده است.
هدف اصلی این سامانه، شبیه‌سازی محیطی است که کاربران بتوانند درخواست‌های پردازشی سنگین (مثل آموزش مدل‌های هوش مصنوعی) را ثبت کنند و سیستم به صورت هوشمند این درخواست‌ها را مدیریت کند.

---

## ✨ قابلیت‌ها و ویژگی‌ها

* **معماری مدرن:** استفاده از فریم‌ورک سریع و قدرتمند **FastAPI**.
* **داکیومنت خودکار:** دارای پنل تست گرافیکی (Swagger UI) جهت ارائه راحت به استاد/مشتری.
* **مدیریت کاربران:** سیستم ثبت‌نام کاربران با رمزنگاری امن پسورد (Hashing).
* **پایگاه داده:** استفاده از **SQLAlchemy** و دیتابیس SQLite (بدون نیاز به نصب نرم‌افزار جانبی).
* **اعتبارسنجی داده‌ها:** استفاده از **Pydantic Schemas** برای بررسی دقیق ورودی‌ها و خروجی‌ها.
* **ساختار استاندارد:** کدنویسی تمیز و ماژولار (پوشه‌بندی حرفه‌ای).

---

## 🛠️ تکنولوژی‌های استفاده شده

* **زبان:** Python 3.x
* **فریم‌ورک وب:** FastAPI
* **سرور:** Uvicorn
* **دیتابیس:** SQLAlchemy (ORM)
* **امنیت:** Passlib (Bcrypt)

---

## 🚀 راهنمای نصب و اجرا (Installation)

برای اجرای پروژه روی سیستم خودتان، مراحل زیر را به ترتیب انجام دهید:

### ۱. دریافت پروژه
ابتدا فایل‌های پروژه را دانلود کنید یا دستور زیر را در ترمینال بزنید:
```bash
git clone <آدرس-ریپازیتوری-شما>
cd gpu-service-manager

۲. نصب پیش‌نیازها
مطمئن شوید که محیط مجازی (venv) فعال است، سپس کتابخانه‌های مورد نیاز را نصب کنید:

pip install -r requirements.txt


۳. مشاهده خروجی
بعد از دیدن پیام موفقیت‌آمیز در ترمینال، مرورگر خود را باز کنید و به آدرس زیر بروید:

پنل مدیریت (Swagger UI): http://127.0.0.1:8000/docs


نحوه تست (برای ارائه)
۱. پس از اجرای برنامه، وارد آدرس /docs شوید.
 ۲. از بخش Users، روی گزینه /users/register کلیک کنید. 
 ۳. دکمه Try it out را بزنید و یک نام کاربری و رمز عبور وارد کنید. 
 ۴. دکمه Execute را بزنید تا کاربر در دیتابیس ذخیره شود.

